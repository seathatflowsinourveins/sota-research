# SOTA Handoff — Research-Architecture v3.1 → next session (2026-05-29)

## Status at a glance
- **Branch:** `claude/gifted-shannon-d8930c` (pushed to `origin`). Base = `origin/main` (`7a0a806`). **PR target: `main`** (clean 19-commit diff; `feature/research-arch-v3` has diverged at `6c078f50` — do NOT target it without a rebase).
- **State:** **229 tests pass, `biome check .` clean (45 files), tree clean.** 19 commits `ef02772..d18180a`.
- **QC:** GPT-5.5 @ xhigh consensus → **PASS / ship-ready** ([qc-manifest-2026-05-29.md](./qc-manifest-2026-05-29.md)).
- **Shipped:** R1 persist decisions.jsonl+scan-md · R3 gap-fit+pathway gates live · R2 engine↔workflow envelope binding · R6 verdict-ranking · R7 Scorecard probe_status · R9 fork/mirror canonicalization · R4-safe honest source-status · R13 rubric-contract test. Plus code-review resolution (C1/S2/S3/S1/S4/C2 + bootstrap safety-forwarding) and SOTA-reference grounding (4 repos → `inventory/references.md` + `patterns/`).

## Where everything lives (read these first next session)
- **Source of truth for "what & why":** [convergence-2026-05-29-research-arch-grounding.md](./convergence-2026-05-29-research-arch-grounding.md)
- **Deferred decisions + revive-preconditions:** [adr-2026-05-29-deferred-enhancements.md](./adr-2026-05-29-deferred-enhancements.md)
- **QC sign-off:** [qc-manifest-2026-05-29.md](./qc-manifest-2026-05-29.md)
- **Reference map (R# → grounding SOTA repo):** [inventory/references.md](../../inventory/references.md)
- **Slice tracker (RESUME-HERE):** [plan](../superpowers/plans/2026-05-29-convergence-wiring-plan.md)
- **Memory:** `architecture-v3.1-convergence-wiring.md` + `deep-audit-backlog.md`.

## Deferred backlog — priority-ordered, each with its revive-precondition
1. **Live multi-source discovery fan-out (G1 — HIGHEST VALUE).** 7/8 phase-1 sources are workflow/MCP-layer stubs; `sourceStatus` makes that honest. Wiring real sources (RAG-MCP relevance-routing is the reference; gpt-researcher's `MCPToolSelector` + deterministic fallback is the grounded pattern in `patterns/assafelovic/gpt-researcher/sota-distill.md`) **un-defers the Top-N matrix (R5)** by producing ≥2 scored same-category candidates per scan. Lives in the live `SKILL.md` workflow, not the script.
2. **Comparative Top-N matrix (R5).** Build-not-borrow (no surveyed agent ships one). Revive once #1 routinely yields ≥2 scored same-category candidates. Ship only the pure-projection table (fields `routeDecision`/`decision-log` already produce).
3. **Honeypot / active-malware detection (HARD-SAFETY, A.8).** Still a stub; no external convergence source found. `KNOWN_BACKLOG` in `rubric-contract.mjs`.
4. **Weight auto-tuning / judge calibration (G5 second half).** Blocked on a labeled outcome corpus — which R1's `decisions.jsonl` now begins accumulating. Keep `outcome.mjs computeDimensionAccuracy` at `calibrated:false` until labeled `value_label`s + holdout exist. When ready, add `run_id`-based cross-run dedup (C2 laid the keys).
5. **R8 agreement-scalar / R10 evidence-compression / R11 groundedness / R14 registry** — ADR-documented, each with a revive trigger.

## Gotchas for the next session
- **Worktree:** you're in a Claude Code worktree; it commits to `claude/gifted-shannon-d8930c` cleanly as sole writer. `.git` is a file — write commit messages to a temp file + `git commit -F` (avoid backticks in `-m`).
- **Codex model:** `~/.codex/config.toml` pins `gpt-5.5 @ xhigh` (confirmed via session telemetry). The model self-describes as "GPT-5" — trust the config/telemetry, not the self-report.
- **Discipline (the repo's disease):** over-specification-vs-implementation. WIRE what's designed; don't design more. The R13 rubric-contract test (`scripts/lib/rubric-contract.mjs`) enforces declared==wired — keep `KNOWN_BACKLOG` honest.
- **Soft-gate is sacred:** only SAFETY hard-rejects; everything else caps to STUDY/REFERENCE.

## ▶ Ready-to-paste NEXT-SESSION PROMPT

```
Continue the sota-research v3.1 program. First read docs/research/HANDOFF-2026-05-29.md +
docs/research/adr-2026-05-29-deferred-enhancements.md + the plan's RESUME-HERE, then
`git -C C:/SEA/src/sota-research/.claude/worktrees/gifted-shannon-d8930c log --oneline -8`
and `npm test` (confirm 229 green) before changing anything.

Goal: implement deferred item #1 — LIVE multi-source discovery fan-out (gap G1) — the highest-value
unlock (it un-defers the R5 Top-N matrix). Ground it in the patterns already recorded:
patterns/assafelovic/gpt-researcher/sota-distill.md (MCPToolSelector relevance-rank + deterministic
fallback floor) and RAG-MCP / MCP-Zero in the convergence doc. Constraints:
- The live runtime is the .claude/skills/sota-research/SKILL.md WORKFLOW (the .mjs are scaffolds);
  wire real MCP sources (exa/tavily/jina/brave/semantic-scholar/github) into the workflow's phase-1
  fan-out, returning family-tagged candidates into phase2Convergence (which already canonicalizes).
- Keep the soft-gate sacred + the engine authoritative (emit the routeDecision decision-envelope).
- TDD each slice; keep 229+ tests green + Biome clean; commit per verified slice; update RESUME-HERE.
- Then run a GPT-5.5 @ xhigh convergence QC (codex exec; confirm model from session telemetry) and
  append to docs/research/qc-manifest. If R5 becomes feasible (≥2 scored same-category candidates per
  scan), build the pure-projection Top-N matrix per ADR-R5.
Use a multi-angle research workflow with Opus 4.8 subagents only if a genuine discovery/parallel need
arises; otherwise stay single-thread. Open/append to the PR into main when green.
```
