# SOTA References — architecture grounding (2026-05-29)

> The high-quality SOTA repos whose patterns GROUND this architecture's design, recorded as durable REFERENCE entries (the operator ask: "architecture with installed high-quality SOTA repos and references"). Ingested via repomix + deepwiki (grounding workflow run `wvsbmfd1e`). These are research-agent / MCDA-class repos → **REFERENCE/STUDY** (no direct Claude-Code runtime surface — the architecture studies their patterns, it does not install them; the D3 adoption-pathway veto routes them to STUDY by design). Full convergence context + citation discipline: `docs/research/convergence-2026-05-29-research-arch-grounding.md`.

## Reference map — which shipped enhancement each SOTA source grounds

| Enhancement | Grounded by | Reason |
|---|---|---|
| R1 — persist decisions.jsonl + scan-<ts>.md (persistence-as-system-of-record) | langchain-ai/open_deep_research, Alibaba-NLP/DeepResearch, HKUDS/CLI-Anything | ODR's override_reducer-annotated notes/raw_notes reduced into the final report = accumulated notes as authoritative system-of-record; DeepResearch writes one structured result per exit with a discrete termination reason (machine-readable record, not inferred); CLI-Anything's file-locked JSON session state (_locked_save_json, open r+/lock/truncate) is the same durable append-with-integrity discipline as decisions.jsonl. This is the doc's stated dominant lever (grounding doc line 37: persistence-as-system-of-record). |
| R2 — bind the live workflow to a decision-envelope + anti-improvisation contract (engine is authoritative) | HKUDS/CLI-Anything, langchain-ai/open_deep_research, Alibaba-NLP/DeepResearch | CLI-Anything is the tightest fit: every executable surface (generator command, validator, test contract) opens with 'Read HARNESS.md First ... Do not improvise / single source of truth' — the spec IS the engine, commands are thin conformance harnesses. ODR's typed Command[Literal[next_nodes]] makes control flow declared-by-the-engine not improvised, with allow_clarification governing routing. DeepResearch's structured termination envelope makes 'why did it stop / what did it produce' a first-class engine verdict, not improvisation. |
| R3 — relevance/gap-fit + adoption-pathway gates (route INSTALL vs STUDY by real runtime surface) | langchain-ai/open_deep_research, HKUDS/CLI-Anything, assafelovic/gpt-researcher | ODR's clarify_with_user is a cheap admission/scope gate (route to __end__ vs write_research_brief) BEFORE expensive fan-out + explicit supervisor exit criteria — routes work before committing cost. CLI-Anything's HARNESS Phase 1 'identify the backend engine' + fail-closed 'real software is a hard dependency, tests fail not skip' routes by actual runtime surface (INSTALL only when a real backend exists, else demote) — the closest analog to pathwayFromCategory's null=study-not-install. gpt-researcher's MCPToolSelector ranks candidates by fit-to-query before binding only the selected subset downstream. |
| R6 — rank by the engine verdict, not popularity/appearance-count | assafelovic/gpt-researcher, langchain-ai/open_deep_research | gpt-researcher is the direct ground: tool_selector ranks by relevance_score (fit-to-query), explicitly NOT by appearance/frequency count, returning index+name+relevance_score+reason. ODR supplies the typed-engine-verdict frame (Command-routed control flow decided by the engine). Verified live: decision.mjs:54 decisionRankKey keys on [action tier, final score, coverage, marginal value], never a raw popularity dimension. |
| R7 — OpenSSF Scorecard probe_status (outage caps confidence, never rejects) | HKUDS/CLI-Anything, assafelovic/gpt-researcher | CLI-Anything is the literal analog: the rendering escalation ladder (native engine → translated filtergraph → script) and output-verification tiers (exit → magic-bytes → structural → pixel/audio) are graceful confidence-capping degradation rather than hard rejection — mirroring probe_status capping confidence (evidence.mjs capConfidenceForProbe, ≤0.5) instead of failing closed. gpt-researcher's deterministic pattern-scored fallback (every LLM/provider failure degrades selectivity rather than rejecting/stalling) is a secondary support for degrade-don't-reject; gpt-researcher self-claimed R7 but its fit is the fallback-floor principle, not the Scorecard-specific probe cap. |
| R10 (DEFERRED) — per-worker evidence compression at the scoring->decision seam | langchain-ai/open_deep_research, Alibaba-NLP/DeepResearch | ODR's compress_research is a dedicated per-worker LLM synthesis node at the worker->aggregation seam (returns compressed_research + raw_notes with token-limit retry) — the direct ground, and ODR's keep-the-deleted-deterministic-dedup-in-legacy move is cited verbatim in ADR-R10/R15 as validation for adopting LLM compression for the narrative layer ONLY. DeepResearch's WebResummer compacts history into one evolving summary on a round/token trigger (same per-worker compression discipline). Both are explicitly named in ADR-R10 as the reference designs; R10 is deferred until scoring subagents return rich per-dimension evidence. |
| R13 — a rubric-contract test (declared dims/gates must have a live producer) | HKUDS/CLI-Anything | CLI-Anything's validate.md is the sole and direct ground: 'single source of truth ... every check in this command maps to a requirement in HARNESS.md' is exactly the rubric-contract — every declared dimension/overlay/gate must have a live producer, no orphan standards. Verified live: scripts/lib/rubric-contract.mjs RUBRIC_PRODUCERS maps each declared dim (D1–D11) to a real function ref, with KNOWN_BACKLOG allowlisting the genuinely-unwired (honeypot/weight-tuning/live fan-out). |

**Not grounded by this repo set:** R9 — canonicalize forks/mirrors/aggregators before counting independent source families (grounded by the papers/tools in the convergence doc — e.g. OpenSSF Scorecard for R7, pymcdm/scikit-criteria for R5/R8).

> FINDING (R9 is the one shipped rec with NO grounding repo in this distillation set). R9 (verified shipped: scripts/discover.mjs:144-178 canonicalIdentity / canonicalSourceFamily / countIndependentFamilies — folds a repo+its fork/mirror to one canonical identity and all external aggregators to a single 'external-aggregator' family BEFORE family_count, so aggregator-only can never alone satisfy >=3/>=4 convergence). None of the four distilled repos performs source-family canonicalization or anti-astroturf dedup before counting independent evidence sources — gpt-researcher/ODR/DeepResearch/CLI-Anything are all single-pipeline research agents, not multi-source-trust accounting systems. This matches the grounding doc's own 'Coverage holes' (line 50, no active-malware/anti-astroturf source) and ADR-R15: family-convergence is a deliberate BUILD-not-borrow, and its deterministic anti-astroturf accounting is the project's own invention that convergence research deliberately does NOT replace. In the canonical convergence doc (lines 16,21,36) R9 is grounded by ossf/scorecard + ossf/criticality_score (OpenSSF's 'activity != criticality' caveat) and the family-convergence ADR — repos that are OUTSIDE this 4-distillation set, which is why R9 reads as ungrounded *here*. So R9 ungrounded is a property of THIS distillation slice, not a true architecture gap.  Two self-report corrections folded in: (1) gpt-researcher self-claimed R7, but its actual transferable pattern (deterministic top-k fallback floor on any LLM/provider failure) grounds the degrade-don't-reject *principle*; the Scorecard-specific probe_status cap is more precisely grounded by CLI-Anything's explicit verification-tier ladder. Recorded gpt-researcher as secondary R7 support, CLI-Anything as primary. (2) Authoritative rec definitions cross-checked against docs/research/convergence-2026-05-29-research-arch-grounding.md (rec table lines 56-72) and the wiring plan (all S1–S8 marked DONE: Tier 1 R1/R3/R2/R6 + Tier 2 R7/R9/R4-safe/R13 shipped; R10 deferred per ADR-R10). R10 was included since the prompt listed it among the eight, flagged (DEFERRED) — it is grounded but not live-wired. The prompt's rec list omits R4/R5/R8/R11/R12/R14/R15 (also deferred/ADR-only).  File refs (all absolute): C:\SEA\src\sota-research\.claude\worktrees\gifted-shannon-d8930c\docs\research\convergence-2026-05-29-research-arch-grounding.md ; ...\docs\research\adr-2026-05-29-deferred-enhancements.md ; ...\docs\superpowers\plans\2026-05-29-convergence-wiring-plan.md ; ...\scripts\lib\decision.mjs (R6 decisionRankKey @ line 54) ; ...\scripts\discover.mjs (R9 canonicalIdentity @ line 144) ; ...\scripts\lib\evidence.mjs (R7 capConfidenceForProbe) ; ...\scripts\lib\rubric-contract.mjs (R13 RUBRIC_PRODUCERS).

## Ingested reference repos

### assafelovic/gpt-researcher
- URL: https://github.com/assafelovic/gpt-researcher · evidence: strong · grounds: R3, R6, R7
- Pattern → `patterns/assafelovic/gpt-researcher/sota-distill.md`
- Two-tier routing gate with a deterministic floor: an LLM relevance-ranker picks a top-k of candidates by fit-to-query (scored, with reasons), but EVERY failure mode — empty response, unparseable JSON, zero picks, exception — falls through to a deterministic pattern-scored top-k guaranteed non-empty when any candidate m…

### langchain-ai/open_deep_research
- URL: https://github.com/langchain-ai/open_deep_research · evidence: strong · grounds: R2, R3, R10, R1
- Pattern → `patterns/langchain-ai/open_deep_research/sota-distill.md`
- Typed-Command node routing with an upfront admission gate plus capped map-reduce: every node returns Command[Literal[next_nodes]] so control flow is declared in the type, not improvised. clarify_with_user is a cheap scope/admission gate that routes to __end__ (ask) or write_research_brief (proceed) BEFORE any expensive…

### HKUDS/CLI-Anything
- URL: https://github.com/HKUDS/CLI-Anything · evidence: strong · grounds: R2, R13, R3, R7, R1
- Pattern → `patterns/HKUDS/CLI-Anything/sota-distill.md`
- A single authoritative spec file (HARNESS.md) that EVERY executable surface is contractually bound to read-first and forbidden to improvise around: the generator command, the validator, and the test contract all open with "Read HARNESS.md First ... Do not improvise" / "single source of truth ... every check maps to a r…

### Alibaba-NLP/DeepResearch
- URL: https://github.com/Alibaba-NLP/DeepResearch · evidence: strong · grounds: R2, R1, R7
- Pattern → `patterns/Alibaba-NLP/DeepResearch/sota-distill.md`
- Bounded-agent control loop with a structured termination envelope + graceful forced-answer degradation. The loop is governed by multiple independent budget caps (call-count, wall-clock, token) checked every round; NO cap is a crash — on the token cap it salvages a partial <answer> and, failing that, emits termination='…
